# 트랜잭션 랜딩 가이드

> 솔라나(Helius)와 이더리움(Tatum)에서 트랜잭션을 직접 보내보는 초보자용 개념정리 + 실습 체크리스트
> 반드시 테스트넷(Solana devnet / Ethereum Sepolia)에서 연습하세요.

---

## 0. 왜 이 실습을 하는가

**결론:** 트랜잭션이 만들어져서 체인에 박히는 전체 파이프라인을 손으로 직접 통과시켜보고, 각 단계에서 "왜, 어떻게" 실패하는지 구분할 수 있게 되는 것이 목적입니다.

개념만 읽어서는 "블록에 들어간다"는 말이 추상적으로 느껴지지만, 직접 성공 1번·실패 3~4번을 만들어보면 그 순간부터 감이 잡힙니다. 배우고 나면 아래를 할 수 있습니다.

1. **지갑 → RPC → 체인** 흐름을 코드로 직접 이해 — 이후 어떤 dApp·봇·자동화 스크립트를 만들든 반드시 필요한 최소 기반입니다.
   *비유: 운전면허 실기시험처럼, 도로 이론을 아는 것과 실제로 차를 몰아본 것은 다릅니다.*
2. 에러 메시지 하나만 보고 **"접수거부 / 대기소멸 / 리버트 / 롤백"** 중 무엇인지 즉시 분류 — 실무에서 트랜잭션이 안 될 때 원인을 빠르게 좁히는 능력입니다.
3. **Helius / Tatum / GetBlock / Etherscan**이 각각 무슨 역할인지 헷갈리지 않고, 지금 필요한 서비스를 바로 고를 수 있게 됩니다.
4. "분명 실패했는데 왜 잔액이 줄었지?"(리버트) 같은 실무형 트러블슈팅에 당황하지 않습니다.
5. 여기서 다진 기반 위에 자동화 송금 스크립트, 민팅 봇, 결제/정산 연동 같은 실제 프로젝트로 바로 넘어갈 준비가 됩니다.

---

## 1. 한 줄 요약

| # | 항목 | 결론 |
|---|---|---|
| 1 | 전체 흐름 | 지갑이 **서명** → RPC 노드(Helius/Tatum)에 **요청 전송** → 대기열(mempool 등)에서 검증자·채굴자가 **블록에 포함** → 컨펌이 쌓여 **확정(finality)** |
| 2 | 서명은 누가? | Helius·Tatum이 아니라 **내 지갑(개인키)**이 함. RPC 업체는 "이미 서명된 것"만 전달하는 심부름꾼 |
| 3 | 실패 vs 리버트 vs 롤백 | ① 접수 자체 거부(수수료 X) ② 블록엔 들어갔지만 로직 실패=리버트(이더리움은 가스비 나감) ③ 들어갔다 취소=롤백/리오그(파이널리티 전에만 이론상 가능, 매우 드묾) |
| 4 | Helius | 솔라나 전용 RPC + 확장 API 제공업체 (서명 대행 아님) |
| 5 | Tatum | 이더리움 등 130+ 체인용 RPC 게이트웨이, API 키로 인증 |
| 6 | Etherscan | RPC가 아니라 **조회 전용** 블록 탐색기 API (잔액·내역 확인용) |
| 7 | GetBlock | Tatum과 같은 부류의 RPC 노드 임대 서비스(경쟁 인프라 업체) |
| 8 | "채굴자" | 정확히는 이더리움은 2022년 이후 채굴자가 아니라 **검증자(validator)**가 블록을 만듦. 솔라나도 검증자 |

> ⚠️ **안전 수칙** — 아래 체크리스트는 전부 테스트넷(솔라나 devnet, 이더리움 Sepolia) 기준입니다. 진짜 돈이 들어있는 메인넷 지갑으로 실패 케이스를 실험하지 마세요. 테스트넷 코인은 무료 faucet에서 받을 수 있습니다.

---

## 2. 초보자를 위한 개념정리

체인이 달라도 이 개념들의 뼈대는 같습니다. 은행 창구에 비유하면서 하나씩 짚어봅니다.

### RPC (Remote Procedure Call)
**결론:** 내가 블록체인 원장을 직접 만지는 게 아니라, "창구 직원"에게 요청을 던지는 것

- 비유 — 은행 창구에 "이체해주세요"라고 요청서를 내미는 것과 같습니다. 내가 직접 금고(원장)를 열 수 없고, 창구 직원(RPC 노드)이 대신 처리해줍니다.
- Helius, Tatum, GetBlock은 모두 이 "창구"를 대신 운영해주는 회사입니다. 내가 직접 노드를 설치·운영하지 않아도 되게 해줍니다.

### 서명(Signing)
**결론:** 개인키로 "이 요청은 진짜 내가 보낸 것"이라는 도장을 찍는 것

- 비유 — 계약서에 인감도장을 찍는 것. 도장(서명) 없는 계약서는 은행도 받아주지 않습니다.
- **중요한 오해 바로잡기**: Helius나 Tatum은 서명 서비스가 아닙니다. 서명은 내 지갑(브라우저 확장, SDK의 Keypair 등)이 로컬에서 개인키로 하고, RPC 업체는 `이미 서명된` 트랜잭션을 받아서 네트워크에 전달만 합니다.

### Blockhash / Nonce — "유효기간표"
**결론:** 트랜잭션에는 유통기한이나 순번표가 붙어 있어서, 늦게 내면 반려됩니다

- 솔라나는 `blockhash`를 씁니다 — 발급 후 약 60~90초(150 블록) 안에만 유효. 예: 12시 00분 00초에 발급받은 요청서를 12시 01분 30초에 내면 `BlockhashNotFound`로 거부됩니다.
- 이더리움은 `nonce`(계정별 순번)를 씁니다 — 번호표 뽑기와 같아서, 3번 트랜잭션이 아직 안 끝났는데 5번을 먼저 내면 순서가 밀립니다.

### 가스비 / 우선순위 수수료
**결론:** 택배의 "빠른 배송" 추가 요금과 같습니다 — 더 내면 더 빨리 블록에 들어갑니다

- 이더리움: gas price가 시세보다 낮으면(예: 남들은 20 gwei 내는데 나는 1 gwei) 몇 시간이고 `pending`에 머물 수 있습니다.
- 솔라나: priority fee(micro-lamports 단위)를 안 붙이면, 네트워크가 붐빌 때 뒤로 밀리거나 아예 안 들어갈 수 있습니다.

### 멤풀(mempool) / 대기열
**결론:** 서명된 트랜잭션이 블록에 들어가기 전 잠시 머무는 "대기실"

- 비유 — 은행 창구 앞 번호표 대기실. 아직 순서가 안 됐을 뿐, 요청 자체는 접수된 상태입니다.
- 솔라나는 전통적 의미의 멤풀이 크지 않고 리더(현재 블록 생성 담당 검증자)에게 직접 전달되는 구조에 가깝지만, 개념적으로는 동일하게 "처리 대기 중"이라고 이해하면 됩니다.

### 블록에 포함 (채굴 / 검증)
**결론:** 검증자(예전엔 채굴자)가 대기 중인 트랜잭션들을 골라 "이번 페이지(블록)"에 적어 넣는 행위

- 비유 — 하루치 은행 거래를 마감 장부 한 페이지에 옮겨 적는 것. 그 페이지(블록)에 내 요청이 옮겨 적히면 "블록에 들어갔다(랜딩)"고 합니다.
- 정확한 용어: 이더리움은 2022년 "머지(The Merge)" 이후 **채굴자(PoW)가 아니라 검증자(PoS)**가 블록을 만듭니다. 솔라나도 처음부터 검증자(그중 그 순간의 "리더")가 블록을 생성합니다. "채굴자"라는 말은 이제 비트코인 등 PoW 체인에만 정확히 맞는 표현입니다.

### 컨펌(Confirmation) / 파이널리티(Finality)
**결론:** "몇 명이 도장을 더 찍었는가" — 많이 찍힐수록 뒤집힐 확률이 낮아집니다

- 이더리움: 블록 1개에 들어가면 1 컨펌. 보통 12~15 컨펌 정도 쌓이면 실무적으로 안전하다고 봅니다.
- 솔라나: `processed` → `confirmed` → `finalized` 3단계. `finalized`가 되면 사실상 절대 안 뒤집힙니다.

### 실패(Fail) vs 리버트(Revert) vs 롤백(Rollback/Reorg)
**결론:** 셋은 "돈이 나가는지"와 "언제 걸렸는지"가 다릅니다 — 4번 섹션 표에서 실전 예시로 구분합니다

- 실패 = 창구에서 접수 자체를 거부(서명 오류, 잔액 부족, 유효기간 만료 등) → 보통 수수료 안 나감.
- 리버트 = 접수·기록은 됐는데 계약 조건이 안 맞아 "실행 취소" → **이더리움은 가스비가 나갑니다.** 솔라나는 트랜잭션 자체가 원자적(all-or-nothing)이라 실패 시 보통 기본 수수료만 나가고 명령어 효과는 전부 취소됩니다.
- 롤백/리오그 = 블록에 들어가고 컨펌까지 됐는데, 네트워크가 다른 갈래(포크)를 채택하면서 그 블록 자체가 통째로 사라짐 → 파이널리티 전에만 이론상 가능, 매우 드묾.

---

## 3. 오늘 다룰 서비스 지도

이름은 비슷해 보여도 역할이 다릅니다. 특히 **"쓰기(트랜잭션 전송)"**가 되는지 vs **"읽기(조회)"**만 되는지가 핵심 구분선입니다.

| 서비스 | 체인 | 정체 | 트랜잭션 전송(쓰기) | 잔액/내역 조회(읽기) |
|---|---|---|---|---|
| **Helius** | Solana | RPC + 확장 API(웹훅, 인덱싱 등) | 가능 — `sendTransaction` | 가능 |
| **Tatum** | 이더리움 등 130+ | RPC 게이트웨이 (API 키 인증, 자동 노드 라우팅) | 가능 — `eth_sendRawTransaction` | 가능 |
| **GetBlock** | 이더리움 등 다수 | RPC 노드 임대(= Tatum과 같은 부류, 경쟁 인프라 업체) | 가능 | 가능 |
| **Etherscan** | Ethereum | 인덱싱된 블록 탐색기 API | 주력 아님 (조회 전용 성격) | 주력 — 잔액·내역·컨트랙트 소스 |

한 줄 정리 — **Helius/Tatum/GetBlock = "요청을 처리해주는 창구(RPC)"**, **Etherscan = "장부를 구경하는 창(탐색기)"**. 트랜잭션을 실제로 보내려면 반드시 RPC 쪽을 써야 합니다.

---

## 4. 실습 체크리스트 — 솔라나 (Helius)

devnet 기준. 순서대로 체크하면서 진행하세요.

### A. 준비
- [ ] Helius 가입 → API 키 발급 — 무료 티어로 충분. devnet용 RPC URL 형태: `https://devnet.helius-rpc.com/?api-key=...`
- [ ] devnet 지갑(키페어) 생성 — CLI(`solana-keygen`) 또는 SDK에서 생성. 개인키는 로컬에만 보관 — Helius에는 절대 안 보냄
- [ ] devnet SOL faucet으로 잔액 확보 — 실수해도 실제 돈이 나가지 않는 테스트 코인

### B. 요청 던지기 (성공 케이스)
- [ ] `getLatestBlockhash` 호출 — blockhash + lastValidBlockHeight(유효기간) 받아오기
- [ ] 트랜잭션 조립 — 보낼 계정 · 받을 계정 · 금액 · 방금 받은 blockhash 포함
- [ ] 로컬에서 개인키로 서명 — Helius가 서명하는 게 아니라 내 키페어가 함 (이 단계가 "network 서명"의 실체)
- [ ] `sendTransaction`으로 Helius에 던지기 — 반환값 = 트랜잭션 signature(영수증 번호)
- [ ] signature로 상태 확인 — `getSignatureStatuses` / `confirmTransaction`으로 confirmed → finalized 되는지 지켜보기

### C. 일부러 실패시켜 보기 (핵심 파트)
- [ ] 잔액 0인 지갑으로 전송 시도 — 예상 에러: `InsufficientFundsForFee`
- [ ] blockhash를 90초 이상 묵혀뒀다가 전송 — 예상 에러: `BlockhashNotFound` (유효기간 만료)
- [ ] 서명 없이(혹은 잘못된 서명으로) 전송 — 예상 에러: `InvalidSignature` / `MissingSignature`
- [ ] 각 에러 메시지를 실제로 읽고 6번 섹션 표와 매칭 — "접수 자체 거부"인지 "실행 후 실패"인지 분류해보기

### D. 롤백 관찰(사실상 불가에 가까움)
- [ ] `simulateTransaction`으로 사전 시뮬레이션 결과 확인 — 실제 전송 전에 성공/실패를 미리 예측
- [ ] 원자성(all-or-nothing) 확인 — 여러 명령어 중 하나만 실패해도 전체가 취소된다는 점을 직접 실패 케이스로 확인

---

## 5. 실습 체크리스트 — 이더리움 (Tatum)

Sepolia 테스트넷 기준.

### A. 준비
- [ ] Tatum 가입 → API 키 발급 — RPC 게이트웨이 URL 확인: 예) `https://ethereum-sepolia.gateway.tatum.io/<API키>`
- [ ] Sepolia 테스트넷 지갑 생성 — 개인키는 로컬 보관, Tatum에도 안 보냄
- [ ] Sepolia ETH faucet으로 잔액 확보

### B. 요청 던지기 (성공 케이스)
- [ ] `eth_getTransactionCount`으로 현재 nonce 확인
- [ ] `eth_gasPrice` / `eth_estimateGas`로 가스 정보 조회
- [ ] 로컬에서 개인키로 서명 → raw transaction 생성 — Tatum도 서명 대행 안 함, 서명은 내 몫
- [ ] `eth_sendRawTransaction`으로 Tatum에 던지기 — 반환값 = 트랜잭션 해시(tx hash)
- [ ] `eth_getTransactionReceipt`로 확인 + Etherscan(Sepolia)에서 같은 해시 검색 — status: 1 = 성공, status: 0 = 실패(리버트)

### C. 일부러 실패시켜 보기 (핵심 파트)
- [ ] nonce를 일부러 과거값으로 전송 — 예상 에러: "nonce too low"
- [ ] gas price를 시세보다 훨씬 낮게 설정 — 즉시 거부되진 않고 pending에 오래 머무는 경우가 많음("underpriced")
- [ ] 컨트랙트 호출 조건을 일부러 깨서 리버트 유도 — receipt status 0 확인 + **가스비는 소모됐는지** 지갑 잔액으로 직접 확인

### D. 대체(Replace) & 롤백 관찰
- [ ] pending 상태인 tx를 같은 nonce + 더 높은 gas로 재전송 — "speed up / cancel"의 원리 = nonce가 같으면 먼저 채택된 쪽만 유효
- [ ] 컨펌 수가 적을 때 블록 해시가 바뀌는지 몇 블록 지켜보기 — 파이널리티 전엔 이론상 reorg 가능, 후엔 사실상 불가능하다는 점을 눈으로 확인

---

## 6. "안 되는 거" 구분표 — 4가지 유형

에러가 나면 우선 이 표에서 어느 칸에 속하는지부터 분류하세요. (Helius 공식 가이드: "진단이 먼저, 재시도는 그다음")

| 유형 | 언제 발생 | 수수료 나가나? | 솔라나 예시 | 이더리움 예시 |
|---|---|---|---|---|
| ① 접수 거부 | 노드가 애초에 받아주지 않음 | 안 나감 | `InvalidSignature`, `AccountNotFound` | 서명 오류, 잘못된 raw tx 포맷 |
| ② 대기하다 소멸 | 유효기간·수수료 문제로 방치되다 사라짐 | 보통 안 나감 | `BlockhashNotFound`(유효기간 만료) | gas too low로 영구 pending, nonce too low |
| ③ 블록엔 포함, 실행은 실패(리버트) | 조건 불충족으로 로직이 되돌려짐 | **나감!** | `Custom(N)` 프로그램 에러, `ComputationalBudgetExceeded` | receipt status 0 (require 실패 등) |
| ④ 들어갔다 취소(롤백/리오그) | 네트워크가 다른 포크를 채택 | 이미 낸 수수료는 보통 그대로 소모 | 파이널리티 전 이론상 가능, 매우 드묾 | 파이널리티 전 이론상 가능, 매우 드묾 |

---

## 7. 자주 헷갈리는 것 바로잡기

**Q. Helius나 Tatum이 내 트랜잭션에 서명도 해주나요?**
A. 아니요. 서명은 개인키를 가진 내 지갑(클라이언트)이 로컬에서 합니다. Helius·Tatum·GetBlock은 이미 서명된 트랜잭션을 네트워크로 전달하는 "RPC 창구"일 뿐입니다. 개인키를 이런 서비스에 보내면 안 됩니다.

**Q. Etherscan에 요청을 보내면 트랜잭션이 전송되나요?**
A. Etherscan API는 잔액·트랜잭션 내역·컨트랙트 소스 코드 같은 것을 조회하는 데 특화되어 있습니다. 실제 전송(쓰기)은 Tatum, GetBlock 같은 RPC 서비스의 역할입니다.

**Q. 실패하면 항상 수수료(가스비)는 안 나가나요?**
A. 아닙니다. "접수 자체 거부"(①②)는 대부분 수수료가 안 나가지만, "리버트"(③)는 트랜잭션이 블록에 기록되고 실행까지 됐다가 조건 불충족으로 되돌려진 것이라 이더리움에서는 가스비가 소모됩니다. 솔라나도 서명 검증을 통과한 뒤 실패하면 기본 수수료는 나갈 수 있습니다.

**Q. 블록에 한 번 들어가면 끝난 건가요?**
A. 완전히는 아닙니다. `finalized`(이더리움 기준 충분한 컨펌) 이전에는 이론상 그 블록 자체가 포크 재구성(reorg)으로 사라질 수 있습니다. 실전에서는 컨펌 수가 쌓일수록 안전성이 기하급수적으로 높아지고, finalized 이후엔 사실상 절대 안 뒤집힙니다.

**Q. "채굴자가 블록을 만든다"는 말이 아직도 맞나요?**
A. 이더리움은 2022년 "머지(The Merge)"로 채굴(PoW) 방식을 버리고 지분증명(PoS)으로 전환해, 지금은 검증자(validator)가 블록을 만듭니다. 솔라나도 처음부터 검증자 구조입니다. "채굴자"라는 표현이 정확히 맞는 곳은 비트코인 등 여전히 PoW를 쓰는 체인입니다.

---

*참고: Helius sendTransaction/랜딩 가이드, Solana 트랜잭션 에러 코드 문서, Tatum RPC Gateway 문서, Etherscan API 문서를 바탕으로 정리했습니다. 실제 요청 시에는 각 서비스의 최신 공식 문서를 함께 확인하세요.*
